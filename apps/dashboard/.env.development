NEXT_PUBLIC_API_URL=
# Opcional: rótulo enviado em todo request para a API (`X-Dashboard-Environment`), ex.: production, preview.
# NEXT_PUBLIC_DASHBOARD_ENVIRONMENT=production
# Opcional: mesmo domínio do dashboard em prod (onde existe /c/...). Na Vercel use a URL do deploy.
# NEXT_PUBLIC_PUBLIC_LINK_BASE_URL=https://corretor-no-flow-dashboard.vercel.app
API_INTERNAL_URL=http://127.0.0.1:3001
NEXT_ALLOWED_DEV_ORIGINS=*.trycloudflare.com
